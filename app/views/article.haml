%section.container
  .back-link
    %a{href: "#{@back_path}" }
      Back
  %article
    %header
      %h1.article-title
        = @article.title
    %section.article-body
      = @article.fields[:body]
